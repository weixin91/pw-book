# 自托管密码管理器环境配置

# 数据库（SQLite 单文件，自动创建）
DATABASE_URL="file:./data/pwbook.db"

# JWT
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="7d"

# 注册邮箱白名单（可选，逗号分隔，不设置则允许所有邮箱）
ALLOWED_EMAILS=""

# 服务器配置
PORT=3000
NODE_ENV=production
